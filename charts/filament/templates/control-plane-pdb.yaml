{{- if or .Values.control.autoscaling.enabled (gt (int .Values.control.replicas) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "filament.control.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "filament.control.labels" . | nindent 4 }}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      {{- include "filament.control.selectorLabels" . | nindent 6 }}
{{- end }}

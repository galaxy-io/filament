apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "filament.server.serviceAccountName" . }}
  namespace: {{ include "filament.namespace" . }}
  labels:
    {{- include "filament.server.labels" . | nindent 4 }}

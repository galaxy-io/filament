---
title: "MySQL"
description: "Write to MySQL."
icon: "https://cdn.getgalaxy.io/sources/source-icon-mysql-dark.svg"
---

`connectors/mysql/sink` loads each resource into its own typed table with
native MySQL columns.

## Loading

The engine calls `EnsureSchema` once per resource before extraction, which
creates the destination table (adding any new columns to an existing one)
and prepares the resource's load and delete statements. Each batch is then
streamed straight into the table with `LOAD DATA LOCAL INFILE`: the Arrow
columns are rendered as tab-separated lines and handed to the driver as an
in-memory reader, so no row is ever parsed as JSON on either side. Because
`LOCAL` makes the server skip duplicate keys and coerce bad values with a
warning rather than an error, every load checks the rows landed against
the rows sent and the session's warnings, and fails on either.

The server must allow client-side loads (`local_infile=ON`; the default on
managed MySQL, off on a stock community server). Binary columns arrive as
their raw bytes; JSON columns pass through unchanged so nested structure
survives the round trip. The sink pins its session to UTC, matching the
source, so a same-engine `timestamp` column round-trips as the same
instant.

## Write policies

A resumable ingestion type (`SnapshotUpsert`, `Upsert`, `CDC`) upserts by
primary key with `LOAD DATA … REPLACE`, so a re-delivered row from an
at-least-once resume overwrites rather than errors on the unique
constraint. A non-resumable load skips `REPLACE` and truncates the table
first for full-snapshot replace semantics.

CDC batches apply through a different path: consecutive same-op rows
(insert/update vs. delete) are split into maximal runs in arrival order —
so a delete never jumps ahead of the insert it's paired with — and each run
lands as one statement inside one transaction: inserts/updates through the
same `LOAD DATA … REPLACE`, deletes through a per-session temp table of the
run's keys and a join `DELETE`.

## Modes

Supports `SnapshotReplace`, `SnapshotUpsert`, `Append`, `Upsert`, and `CDC`
write policies (see [Replication
modes](/pages/guides/concepts/replication-modes)).

---
title: "Connectors"
description: "The sources and sinks that plug into Filament, and how mature each one is"
icon: "plug"
---

A connector is a source, a sink, or both. Sources read data from an upstream
system, and sinks write it to a destination. Some connectors (PostgreSQL and
MySQL today) ship both, as separate packages (`connectors/postgres/source`,
`connectors/postgres/sink`).

Connectors are registered factories. Each package's `register.go` calls
`registry.RegisterSource` or `registry.RegisterSink` with a name, a maturity
level, and a factory that builds a fresh instance per run. A blank import is
all it takes to make a connector available:

```go
_ "github.com/galaxy-io/filament/connectors/postgres"
```

Every connector's spec describes its identity, config schema, and
capabilities. The API server's `ListConnectors` RPC surfaces the spec to
the catalog UI, so a registered connector shows up with no separate catalog
to maintain.

## Maturity levels

Every connector is registered with one of three maturity levels.

| Level | Meaning |
|---|---|
| `alpha` | Documentation-derived, not yet tested end to end |
| `beta` | Runs end to end. Not verified for every supported object or environment |
| `stable` | Fully production-ready across its supported objects |

## Sources

| Source | Maturity | Modes |
|---|---|---|
| [PostgreSQL](/pages/connectors/sources/postgres) | beta | Full, incremental, CDC |
| [MySQL](/pages/connectors/sources/mysql) | beta | Full, CDC |
| [Attio](/pages/connectors/sources/attio) | alpha | Full |
| [GitHub](/pages/connectors/sources/github) | beta | Full, incremental |
| [Linear](/pages/connectors/sources/linear) | beta | Full, incremental |
| [Notion](/pages/connectors/sources/notion) | beta | Full, incremental |
| [PostHog](/pages/connectors/sources/posthog) | beta | Full, incremental |
| [Resend](/pages/connectors/sources/resend) | beta | Full |
| [Slack](/pages/connectors/sources/slack) | alpha | Full, incremental |
| [Stripe](/pages/connectors/sources/stripe) | beta | Full, incremental |
| [Sample](/pages/connectors/sources/sample) | stable | Full |

The SaaS sources are not separate codebases.
Each is a YAML manifest interpreted by one generic
[HTTP connector](/pages/connectors/building-a-connector/http-manifests).
Adding another SaaS API is
usually a new manifest, not a new connector. The manifest format is
documented in
[HTTP manifests](/pages/connectors/building-a-connector/http-manifests).

## Sinks

| Sink | Maturity | Writes |
|---|---|---|
| [PostgreSQL](/pages/connectors/sinks/postgres) | beta | Typed tables. Replace, append, upsert, and CDC merge |
| [MySQL](/pages/connectors/sinks/mysql) | alpha | Typed tables. Same write policies as the PostgreSQL sink |
| [ClickHouse](/pages/connectors/sinks/clickhouse) | alpha | MergeTree tables. Atomic replace via staging, no CDC |
| [Iceberg](/pages/connectors/sinks/iceberg) | alpha | Parquet tables through an Iceberg catalog. Commits at run end |
| [Amazon S3](/pages/connectors/sinks/aws-s3) | alpha | NDJSON objects, one per resource per run |
| [stdout](/pages/connectors/sinks/standard-output) | stable | NDJSON to standard output, the reference sink |

## Where to go next

<CardGroup cols={3}>
  <Card title="Sources" icon="download" href="/pages/connectors/sources/postgres">
    How each source reads, covering scan strategies, cursors, and CDC.
  </Card>
  <Card title="Sinks" icon="upload" href="/pages/connectors/sinks/postgres">
    How each sink writes, covering schemas, type maps, and atomicity.
  </Card>
  <Card title="Build your own" icon="wrench" href="/pages/connectors/building-a-connector/writing-a-source">
    Implement the `Source` or `Sink` interface and register it.
  </Card>
</CardGroup>

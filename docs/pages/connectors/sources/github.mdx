---
title: "GitHub"
description: "Read repositories, issues, and pull requests from a GitHub organization"
icon: "https://cdn.getgalaxy.io/sources/source-icon-github-dark.svg"
---

The GitHub source reads repositories, issues, and pull requests for one
organization. It is built with an
[HTTP manifest](/pages/connectors/building-a-connector/http-manifests) and is
**beta**. Its resources are stable, though future releases may add columns.

## Configuration

| Field | Scope | Default | Description |
|---|---|---|---|
| `token` | Connection | | Required. Secret. Personal access token or GitHub App installation token. |
| `organization` | Connection | | Required. GitHub organization login. |

## Resources

`repositories` lists all repositories in the organization. For every
repository, Filament then requests its issues and pull requests and adds the
repository name to each row. Both include open and closed items. GitHub's
issues endpoint also returns pull requests, so issue rows retain a
`pull_request` marker that lets you distinguish them.

## Modes

All resources support full reads. If interrupted, a full read of a top-level
resource resumes after its last completed page.

`issues` is the one incremental resource.

| | |
|---|---|
| Cursor field | `updated_at` |
| Request parameter | `since` (query) |
| Comparator | `time` |
| Checkpoint key | `issues_updated_at` |
| Overlap | 300 seconds |

The cursor field is fixed by the manifest. Only a lookback window is
user-configurable, and only because the comparator is `time`. Issues fan out
across repositories but share a single durable watermark for the whole
resource. The 300-second overlap re-reads a window on each run to cover
repositories that changed while an earlier repository was being scanned.
`pull_requests` has no incremental block and is always read in full.

## Behavior

- **Auth**: bearer token from `token` on every request.
- **Pagination**: RFC 5988 `Link` headers (`rel="next"`), with `per_page=100`
  set as a default query parameter.
- **API version**: pinned via `X-GitHub-Api-Version: 2022-11-28`, so response
  fields do not change with GitHub's rolling default.
- **Rate limiting**: no client-side limiter is configured. The engine backs
  off on 429 responses using `Retry-After`.
- Every resource carries a `raw` remainder column preserving fields the
  manifest does not project.
